<Window x:Class="FinalFinanceTrack.AdminResetPassw"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Reset Password" Height="500" Width="800" Background="#FFF9F6F4">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <!-- Text-based Back Button -->
        <Button Margin="10,-2,0,0" Width="54" Height="23"
                Content="Back" Click="Back_Click"
                HorizontalAlignment="Left" VerticalAlignment="Top"
                Background="DarkGray" Foreground="White"
                FontSize="12" Padding="10,0"/>

        <!-- Title for resetting password -->
        <TextBlock Text="Reset your password" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Grid.Row="1" Margin="0,10,0,5"/>

        <!-- Old Password Label and TextBox -->
        <TextBlock Text="Old Password" Grid.Row="3" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        <TextBox x:Name="AdminOldPasswordTextBox" Grid.Row="4" Width="200" Height="30"
                 HorizontalAlignment="Center" Margin="0,5,0,0"/>

        <!-- New Password Label and TextBox -->
        <TextBlock Text="New Password" Grid.Row="5" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        <TextBox x:Name="AdminNewPasswordTextBox" Grid.Row="6" Width="200" Height="30"
                 HorizontalAlignment="Center" Margin="0,5,0,0"/>

        <!-- Password Requirements -->
        <TextBlock Text="Password must be at least 8 characters long, include at least one uppercase letter, one special character (.@§$+-*/\&amp;lt;\&amp;gt;), and one number (0-9)."
                   FontSize="12" Foreground="Gray" Grid.Row="7" HorizontalAlignment="Center" 
                   TextWrapping="Wrap" Margin="0,9,0,16" Width="360"/>

        <!-- Confirm New Password Label -->
        <TextBlock Text="Confirm New Password" Grid.Row="8" HorizontalAlignment="Center" Margin="0,5,0,0"/>

        <!-- Confirm New Password TextBox -->
        <TextBox x:Name="AdminConfirmPasswordTextBox" Grid.Row="9" Width="200" Height="30"
                 HorizontalAlignment="Center" Margin="0,5,0,0"/>

        <!-- Submit Button -->
        <Button Content="Submit" Grid.Row="10" Width="80" Height="25" HorizontalAlignment="Center" Margin="0,10,0,10" Click="SubmitButton_Click"/>
    </Grid>
</Window>
